[vps_servers]
# Replace this IP with your actual server IP if different
37.187.236.161 ansible_user=root 

# Note: You can change 'root' to whichever user has Docker access and SSH keys configured (e.g. jenda/admin)
